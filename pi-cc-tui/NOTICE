# 第三方归属与许可证说明

本包采用 MIT License。

## 当前已包含或改写的第三方实现

- `extensions/editor.ts`、`extensions/startup-header.ts`、`extensions/render-utils.ts`
  - 来源：`Phoobobo/pi-claude-code-tui`
  - 许可证：MIT
  - 说明：项目既有的圆角输入框、启动头和相关渲染工具；本包已根据 Pi 生命周期调整。

## 当前已改写的第三方实现

- `src/config/json-store.ts`、`src/config/cc-tui-config.ts`
  - 设计来源：`lmilojevicc/pi-zentui` / `beautifulrem/pi-sakura-cyberdeck`
  - 许可证：MIT
  - 说明：按 cc-tui 的版本化 schema、权限保留、符号链接目标保留、损坏文件拒绝覆盖和显式归档要求重新实现；未复制原始文件内容。

- `src/ui/text.ts`、`src/ui/box.ts`
  - 设计来源：`mohndoe/pi-tui-extras` 的 `core/text.ts` 与 `components/border-box.ts`
  - 许可证：MIT
  - 说明：按 cc-tui 的 ANSI 安全、ASCII 图标模式、无状态纯函数和 Todo/Diff 模块隔离要求重新实现；未复制原始文件内容。

- `src/ui/scroll.ts`、`src/overlays/scrollable-overlay.ts`、`src/overlays/context-report.ts`、`src/overlays/tool-report.ts`
  - 设计来源：`zenspc/pi-extensions` 的 pi-devtools context overlay 与工具活动诊断
  - 许可证：MIT
  - 说明：按 cc-tui 的 public API、有限摘要、原始工具参数/图片/系统提示不显示和不写入模型上下文要求重新实现；tool 报告仅显示工具名与成功/失败状态，不含参数或结果内容；未复制原始文件内容。

- `src/status/tool-metrics.ts`、`src/status/tps-tracker.ts`
  - 设计来源：`liao666brant/pi-hud-footer` 的每工具成功/失败统计与短窗口 token 速率/工作时长状态。
  - 许可证：MIT
  - 说明：重新实现为纯增量游标聚合器与有界滚动窗口 TPS 跟踪器；tool-metrics 按 session branch 新增条目推进，rewind/fork 时重建，每工具 Map 有 32 项上限，渲染摘要最多 6 项；tps-tracker 采样环有 128 项上限，5 秒窗口计算响应式 TPS，长空闲后关闭工作时长窗口；渲染保持 O(1)；未复制原始文件内容。

- 工具调用、Diff、用户消息和 Thinking 的呈现不再由 CC-TUI 实现或覆盖。
  - 运行时所有者：`pi-tool-display`（MIT，MasuRii）。
  - 集成边界：CC-TUI 不注册内置同名工具覆盖、不 patch 用户或 assistant 消息组件。未来 CC-TUI 的自有工具如需接入 pi-tool-display 的统一工具呈现，可使用 `pi-tool-display/tool-display-api-consumer` 的公开 v1 装饰 API；该 API 不限制 CC-TUI 其他 UI 功能的实现。
## 设计参考（尚未复制代码）

下列项目只作为架构或行为参考；当前包尚未直接复制其实现：

- `lmilojevicc/pi-zentui` / `beautifulrem/pi-sakura-cyberdeck`（MIT）：工具状态轨。
- `juicesharp/rpiv-mono`（MIT）：常驻 Widget 生命周期。
- `liao666brant/pi-hud-footer`（MIT）：状态栏生命周期与短窗口性能指标。
- `Gurpartap/pi-mermaid`（MIT）：有界缓存与宽度降级策略。

当后续实际移植或改写上述项目的代码时，必须在本 NOTICE 中增加具体文件、来源路径、许可证和改写说明。
